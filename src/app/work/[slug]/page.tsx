import { SectionRedirect } from "@/components/routing/section-redirect";

/** Reserved route — redirect until case study pages ship. */
export default function ProjectDetailPage() {
  return <SectionRedirect sectionId="portfolio" />;
}
