import { SectionRedirect } from "@/components/routing/section-redirect";

/** Reserved route — redirect until dedicated Contact page ships. */
export default function ContactPage() {
  return <SectionRedirect sectionId="contact" />;
}
