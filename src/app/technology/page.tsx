import { SectionRedirect } from "@/components/routing/section-redirect";

/** Reserved route — redirect until dedicated Technology page ships. */
export default function TechnologyPage() {
  return <SectionRedirect sectionId="technology" />;
}
