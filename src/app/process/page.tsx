import { SectionRedirect } from "@/components/routing/section-redirect";

/** Reserved route — redirect until dedicated Process page ships. */
export default function ProcessPage() {
  return <SectionRedirect sectionId="process" />;
}
