/**
 * @deprecated Prefer `SiteHeader` from `@/components/navigation`.
 * Re-export kept so existing imports continue to work during migration.
 */
export { SiteHeader as Navbar } from "@/components/navigation/site-header";
